name: Build for Linux
description: 'Build V8 for Linux'

inputs:
  target-arch:
    description: 'Target Architecture'
    default: x64
    required: true

  target-libc:
    description: 'Target C library (glibc or musl)'
    default: glibc
    required: false

runs:
  using: "composite"
  steps:
    - name: Restore CCache
      uses: actions/cache@v5
      with:
        path: .ccache
        key: ${{ runner.os }}-${{ runner.arch }}-${{ inputs.target-arch }}-${{ inputs.target-libc }}:libv8:ccache:${{ github.run_number }}
        restore-keys: |
          ${{ runner.os }}-${{ runner.arch }}-${{ inputs.target-arch }}-${{ inputs.target-libc }}:libv8:ccache:
          ${{ runner.arch == 'X64' && inputs.target-libc == 'glibc' && format('{0}-{1}:libv8:ccache:', runner.os, inputs.target-arch) || '' }}

    - name: Setup Python
      uses: actions/setup-python@v6
      with:
        python-version: '3.11'

    - name: Setup Build Tools
      shell: bash
      run: |
        sudo apt-get update
        sudo apt-get install -yq build-essential ccache curl git jq ninja-build python3 wget xz-utils pkg-config
        sudo update-ccache-symlinks

        echo "/usr/lib/ccache" >> "$GITHUB_PATH"

        ccacheDir="${GITHUB_WORKSPACE}/.ccache"
        test -d "$ccacheDir" || mkdir "$ccacheDir"

        echo "CCACHE_DIR=$ccacheDir" >> "$GITHUB_ENV"

    - name: Install native compiler toolchains
      if: ${{ runner.arch == 'ARM64' || inputs.target-libc == 'musl' }}
      shell: bash
      env:
        V8_TARGET_LIBC: ${{ inputs.target-libc }}
      run: bash deps/setup-linux-toolchains.sh

    - name: Download V8 Source
      shell: bash
      working-directory: ./deps
      run: ./v8_download.sh

    - name: Prepare ARM64 sysroot
      shell: bash
      if: ${{ inputs.target-arch == 'arm64' }}
      working-directory: ./deps/v8
      run: |
        if [ "$(uname -m)" != aarch64 ]; then
          sudo apt-get install -yq g++-aarch64-linux-gnu
        fi
        build/linux/sysroot_scripts/install-sysroot.py --arch=arm64

    - name: Prepare musl sysroot
      if: ${{ inputs.target-libc == 'musl' }}
      shell: bash
      run: bash deps/setup-musl-sysroot.sh

    - name: Compile V8
      shell: bash
      working-directory: ./deps
      run: ./v8_compile.sh ${{ inputs.target-arch }} ${{ inputs.target-libc }}

    - name: Show CCache Status
      shell: bash
      working-directory: ./deps
      run: ccache -s
